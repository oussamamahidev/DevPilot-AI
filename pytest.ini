[pytest]
addopts = -ra
testpaths = tests
markers =
    live: requires the Docker Compose stack to be running
    ingestion: requires Celery, Redis, Ollama embeddings, and Qdrant indexing
    rag: requires generation provider access and indexed documents
    slow: may take more than 30 seconds
    frontend: validates rendered frontend routes
