## Architecture Section

DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.
DevPilot AI architecture uses a modular FastAPI backend, a Next.js frontend, PostgreSQL metadata storage, Redis infrastructure, Docker Compose services, SQLAlchemy models, Pydantic schemas, and clean service boundaries for private document intelligence.

## Authentication Section

DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.
DevPilot AI authentication uses JWT access tokens, password hashing, active user checks, workspace membership validation, owner roles, and authorization dependencies to isolate private workspace data and protect document access.

## Celery Section

DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.
DevPilot AI uses Celery with Redis to process uploaded documents asynchronously. Celery workers receive document processing tasks, extract text, clean content, split chunks, generate embeddings, update document status, and prevent upload requests from blocking while background processing continues.

## Qdrant Section

DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.
DevPilot AI uses Qdrant for vector search. Qdrant stores embedding vectors for document chunks, filters points by workspace, returns semantic nearest neighbors, and supports retrieval for citation grounded answers.

